<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f4f4;
            padding: 20px;
        }

        .coupon-form {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
    </style>
    <title>Add Coupons</title>
</head>

<body>

    <button class="btn btn-secondary" ><a style="color: black;" href="/admin/offers">Back</a></button>
    <div class="container coupon-form">

        <h2>Edit Offers</h2>
        <form action="/admin/submit-edit-offer" method="post">
            <input type="hidden" value="<%=editOffer._id%>" name="editoffer">
            <div class="form-group">
                <label for="offerType">Offer Type:</label>
                <select class="form-control" id="offerType" value="<%=editOffer.offerType%>" name="offerType" required>
                    <option value="categoery" <%= editOffer.offerType === 'categoery' ? 'selected' : '' %>>Category</option>
                    <option value="referal" <%= editOffer.offerType === 'referal' ? 'selected' : '' %>>Referral</option>
                </select>
            </div>
            
            <div class="form-group">
                <label for="offerName">Offer Name:</label>
                <input type="text" class="form-control" id="offerName" value="<%=editOffer.offerName%>" placeholder="Enter offer name" name="offerName" required>
            </div>
       
            <div class="form-group">
                <label for="offerAmount">Offer Amount:</label>
                <input type="number" class="form-control" value="<%=editOffer.offerAmount%>" id="offerAmount" name="offerAmount" placeholder="Enter offer amount" required>
            </div>
            <div class="form-group">
                <label for="startDate">Start Date:</label>
                <input type="date" class="form-control" id="startDate" value="<%=editOffer.startDate.toLocaleDateString('en-CA')%>" name="startDate" required>
            </div>
            <div class="form-group">
                <label for="endDate">End Date:</label>
                <input type="date" class="form-control" id="endDate" value="<%=editOffer.endDate.toLocaleDateString('en-CA')%>" name="endDate" required>
            </div>
            <!-- <div class="form-group">
                <label for="status">Status:</label>
                <select class="form-control" id="status" name="status" required>
                    <option value="Active">Active</option>
                    <option value="Inactive">Inactive</option>
                </select>
            </div> -->
            <button type="submit" class="btn btn-primary">Add Coupon</button>
        </form>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>

</html>
